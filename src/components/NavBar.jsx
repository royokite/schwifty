import React from "react";

function NavBar() {
    return (
        <section>
            <a href="#">Home</a>
            <a href="#">Characters</a>
            <a href="#">Locations</a>
            <a href="#">Episodes</a>
        </section>
    )
}

export default NavBar;